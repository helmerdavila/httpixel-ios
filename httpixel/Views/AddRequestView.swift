import SwiftUI

struct AddRequestView: View {
  var body: some View {
    VStack {
      Text("This is the new request view")
    }
  }
}

struct AddRequestView_Previews: PreviewProvider {
  static var previews: some View {
    AddRequestView()
  }
}
